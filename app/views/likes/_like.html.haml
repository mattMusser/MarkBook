- if policy(Like.new).create?
  %div
    - if like = current_user.liked(bookmark)
      = link_to [bookmark.topic, bookmark, like], class: 'btn btn-md btn-danger', method: :delete do
        %i.fa.fa-thumbs-o-up>
    - else
      = link_to [bookmark.topic, bookmark, Like.new], class: 'btn btn-md btn-primary', method: :post do
        %i.fa.fa-thumbs-up>